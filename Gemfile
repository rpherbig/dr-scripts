source 'http://rubygems.org'

ruby IO.read('./.ruby-version').strip.split('-').first if respond_to?(:ruby)

gem 'firebase'
gem 'rake'

group :test do
  gem 'minitest'
  gem 'rubocop'
  gem 'simplecov'
  gem 'timecop'
end