## Parse steal rooms from YAML by using mapper
## v.01 by Iloru

$checkStealRoom_Debug = true

echo 'test'

steal_settings = YAML.load_file('./scripts/data/base-stealing.yaml') if steal_settings.nil?


steal_settings.each do |key, value|
  value.each do |key, value|
    
    key.each do |key, value|
    echo value if key == "room"
    pause 0.5
    end 
  end
end