=begin
  Suggestions and contributions are welcome: https://github.com/rpherbig/dr-scripts
=end

custom_require(%w(common))

class CombatTrainerSetup
  include DRC

  def initialize(args:[])
    echo '***combat-setup IS DEPRECATED AND WILL SOON BE DELETED. UPDATE SCRIPT REFERENCES AND CALL combat-trainer DIRECTLY***'
    pause 5
    wait_for_script_to_complete('combat-trainer', args)
  end
end

CombatTrainerSetup.new(args: variable.drop(1))
