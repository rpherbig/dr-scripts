=begin
  Suggestions and contributions are welcome: https://github.com/rpherbig/dr-scripts

  Runs in the background to provide safety while you are afk. Will exit on low health or death.
=end

custom_require.call(%w(drinfomon spellmonitor))

loop do
  line = script.gets?
  pause 0.1 unless line
  UserVars.athletics = DRSkill.getrank('Athletics').to_i
  if DRSpells.active_spells['Athleticism']
      UserVars.athletics = UserVars.athletics*1.1
  end
end
